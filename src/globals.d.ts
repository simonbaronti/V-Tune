/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts) — the package.json
// version of the running build, used by the in-app update checker.
declare const __APP_VERSION__: string;
